const Request = require('../Models/Requests');


class RequestService
{
    static async searchByID(requestGUID)
    {
        // logic for query of request
    }

    static async searchByJobGUD(requestGUID)
    {
        // take jobGUID and create request on specific board
    }
}